# Extra deps for train_models.ipynb / mailbox_to_csv.ipynb, on top of requirements.txt
# (this doesn't duplicate torch/numpy/sentence_transformers pins here on purpose --
# a second independent copy is exactly how the health.py/ai_mail_classifier.py
# model-dim drift happened).
#
# Usage: pip install -r requirements.txt -r requirements-train.txt
-r requirements.txt

pandas>=3.0.5
scikit-learn>=1.9.0
imbalanced-learn>=0.14.2
matplotlib>=3.11.1
seaborn>=0.13.2
tqdm>=4.70.0
bs4>=0.0.2
jupyter>=1.1.1

# retrain model monthly/ (IMAP fetch pipeline)
python-dotenv>=1.0

# retrain model monthly/promote.py (push run metrics to the Suspicious
# dashboard) - not pinned elsewhere here, only a transitive dep of
# sentence-transformers/huggingface_hub until now.
requests>=2.32

# retrain model monthly/Re_Train_Model/utils.py training-run monitoring
# (per-epoch loss/accuracy/time + current-vs-previous label sample counts).
# Defaults to WANDB_MODE=offline (no account/API key, writes to ./wandb/
# locally) - see run_monthly_cycle.sh.
wandb>=0.17
