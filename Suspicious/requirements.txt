# LDAP Authentication and Database Connectivity
django==6.0.7
django-auth-ldap==5.3.0
django-mysql==4.19.0
mysqlclient==2.2.8
python-ldap==3.4.7
djangorestframework==3.18.0
django-rest-knox==5.1.0
django-filter==26.1
drf-spectacular==0.30.0
gunicorn==26.0.0
whitenoise==6.12.0
# Environment Configuration
docker==7.2.0

# Parsing and Text Extraction
beautifulsoup4==4.15.0
compoundfiles==0.3
compressed-rtf==1.0.7
extract-msg==0.56.1
pyspf==2.0.14
tldextract==5.3.1
dkimpy==1.1.8
email-validator==2.3.0
# Mail preview: imgkit wraps the wkhtmltopdf binary that ships in the
# runtime image. Replaces the unmaintained eml2png==0.0.2 CLI; see
# mail_feeder/utils/email_preview/eml2png_renderer.py for the renderer.
imgkit==1.2.3

# Natural Language Processing and Similarity Checking
nltk==3.10.2
simhash==2.1.2

# Image and Font Utilities
fontawesomefree==6.6.0

# Web Framework and Application Enhancements
django-import-export==4.4.1

# Request Handling
requests==2.34.2
PyJWT==2.13.0
minio==7.2.20
tenacity==9.1.4
pybreaker==1.4.1

# Email Handling
chardet==7.5.1

# Templating (Jinja2: TheHive challenge + scoring notification emails —
# connectors/contrib/thehive/challenge.py, score_process/.../send_mail/utils.py.
# Previously satisfied transitively; pin it as the direct dependency it is.)
jinja2==3.1.6

# Data Analysis and Machine Learning
numpy==2.5.2
validators==0.35.0
hashid==3.1.4

# Cybersecurity and Incident Response
thehive4py==2.1.0
cortex4py==2.1.0
pymisp==2.5.34.1
vt-py==0.22.0

# Vector store
chromadb==1.5.9

# Logging
python-json-logger==4.1.0  # v4.x changed the API; pin to v3 until we migrate

# Image rendering (cairosvg: SVG→PNG for Outlook email previews)
pillow==12.3.0
cairosvg==2.9.0

# Task Queue
celery[redis]==5.6.3
django-celery-results==2.6.0

# Secrets
hvac==2.4.0

# Observability
# 1.28.0+ needed: opentelemetry-proto pins protobuf<5 below that, which
# conflicts with the >=5.29.6 fix for PYSEC-2026-1805.
opentelemetry-sdk==1.44.0
opentelemetry-instrumentation-django==0.65b0
opentelemetry-instrumentation-requests==0.65b0
opentelemetry-instrumentation-celery==0.65b0
opentelemetry-exporter-otlp-proto-http==1.44.0
