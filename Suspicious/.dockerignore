# VCS
.git
.gitignore

# Docker
Dockerfile
.dockerignore

# Python caches / virtualenvs
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Local DB / secrets (mounted at runtime, never baked into the image)
*.sqlite
*.sqlite3
*.env
settings.json
config.json

# Logs / OS cruft
*.log
.DS_Store
