// Placeholder. In production the container entrypoint overwrites this file
// with the deployment's VITE_* values. In dev it stays empty and the app
// falls back to import.meta.env (.env via the Vite dev server).
window.__ENV__ = {};
