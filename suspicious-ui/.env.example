# Optional: override API base for non-proxy environments.
# If you keep Nginx same-origin proxy (/api -> backend), you can leave this unset.
VITE_API_BASE=/api
VITE_FAVICON=/icons/suspicious-logo.png
